export type CreateReportScheduleState = {
    error: string | null;
    success: string | null;
};

export const initialCreateReportScheduleState: CreateReportScheduleState = {
    error:null,
    success:null,
};